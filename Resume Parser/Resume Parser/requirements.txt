
flask
flask-cors
requests
beautifulsoup4
lxml
python-dotenv
supabase==2.31.0
# playwright  # ⚑ LOCAL DEV ONLY — disabled for free-tier PaaS (too large / needs system chromium).
              # Enable locally with: pip install playwright && playwright install chromium
